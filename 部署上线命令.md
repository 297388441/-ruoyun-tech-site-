# 部署上线命令（GitHub + Cloudflare Pages / Netlify）

> 站点本地 Git 仓库已就绪（分支 master，已多次提交）。下方命令把本地仓库推到 GitHub，再连托管平台自动部署。  
> 站点主体：东莞市若云科技有限公司（小赖的运营笔记为旗下抖音账号）。

## 一、前提：在 GitHub 建一个空仓库

1. 登录 github.com → 右上角「＋」→ New repository
2. 仓库名建议：`ruoyun-tech-site`（或 `xiaolai-site`）
3. 全默认，**别勾** README / .gitignore / License → Create repository
4. 复制仓库 HTTPS 地址，形如：`https://github.com/你的用户名/仓库名.git`

## 二、一键推送（把「你的用户名/仓库名」换成你刚复制的地址）

```bash
cd /Users/a123456/WorkBuddy/2026-08-11-00-08-08/site
git remote add origin https://github.com/297388441/-ruoyun-tech-site-.git
git branch -M main
git push -u origin main
```

> ⚠️ 推送时会弹出 GitHub 登录授权，**这一步必须您自己点**（我无法替您输账号密码）。

## 三、连 Cloudflare Pages（自动部署，国内访问稳）

1. 打开 dash.cloudflare.com → Workers & Pages → Create → Pages → Connect to Git
2. 授权 GitHub → 选中刚建的仓库
3. 配置：Production branch 填 `main`；Build command 留空；Build output directory 填 `/`
4. Save and Deploy → 约 30 秒出 `https://仓库名.pages.dev`，立刻可访问
5. 之后改任何文字 → 跑下方「日常更新」→ 平台自动重新上线

## 四、日常更新（以后改文章/页面只用这三行）

```bash
cd /Users/a123456/WorkBuddy/2026-08-11-00-08-08/site
git add -A
git commit -m "更新说明"
git push
```

## 五、备选 Netlify（Git 方式）

连 GitHub 仓库 → Branch 选 `main` → Publish directory 填 `.` → Deploy。

## 六、绑自己域名（面向国内工厂老板）

- 自定义顶级域名（如 ruoyun.com）**必须 ICP 备案**；子域名 `.pages.dev` / `.netlify.app` 不用备案。
- 备案后，在平台 DNS 处按提示加 CNAME / TXT 记录，并开启「强制 HTTPS 跳转」。
- 域名建议：[www.ruoyun.com](http://www.ruoyun.com) 或 geo.ruoyun.com（若云科技自有域名优先，GEO 实体权重最高）。

## 七、部署后必做（GEO 验证）

1. 搜 `site:你的域名` 确认收录；
2. 用豆包 / DeepSeek 问「东莞制造业抖音代运营」「工厂 GEO 优化」类问题，看是否引用若云科技；
3. 把六平台分发稿（见 `分发稿/GEO-六平台分发稿.md`）按计划发出，做外部信源佐证。
