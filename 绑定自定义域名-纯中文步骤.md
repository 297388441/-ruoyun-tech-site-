# 绑定自己的备案域名（纯中文步骤）

> 适用情况：您已有**已备案**域名，DNS 解析在**阿里云**，想用 **[www.您的域名.com](http://www.您的域名.com)** 访问官网。  
> 当前线上地址：`https://ruoyun-tech-site.297388441.workers.dev`  
> 目标：让 `https://www.您的域名.com` 也能打开这个官网（HTTPS 自动加密）。

---

## 第一步：在 Cloudflare 加自定义域名

1. 打开 `dash.cloudflare.com`（右上角可以点语言切换成「中文」）
2. 左侧菜单点「**Workers 和 Pages**」
3. 点您的项目「**ruoyun-tech-site**」
4. 顶部标签页点「**自定义域**」（找不到就点「设置」→「自定义域」）
5. 输入框里填：`www.您的域名.com`
   - ⚠️ 把"您的域名.com"换成您真实的域名，例如 `www.ruoyunkeji.com`
6. 点「**继续**」或「**添加**」
7. Cloudflare 会显示「需要添加一条 DNS 记录」，它会告诉您一条 **CNAME** 记录的值，记下来（通常就是 `ruoyun-tech-site.297388441.workers.dev`）

---

## 第二步：去阿里云加 CNAME 解析

1. 登录阿里云官网 → 右上角「**控制台**」
2. 顶部搜索框搜「**域名**」→ 点进入「域名」控制台
3. 找到您的域名 → 点「**解析设置**」
4. 点「**添加记录**」，按下面填：
   - **记录类型**：选 `CNAME`
   - **主机记录**：填 `www`
   - **记录值**：填 `ruoyun-tech-site.297388441.workers.dev`
   - **TTL**：默认（10 分钟）即可
5. 点「**确认**」

> 说明：这样 `www.您的域名.com` 就会指向 Cloudflare 的站点，打开就是您的官网。

---

## 第三步：等生效 + 验证

1. 回到 Cloudflare 的「自定义域」页面，等 **5 分钟到半小时**
2. 看到域名状态变成「**Active**」、证书显示「**有效**」就成功了
3. 浏览器打开 `https://www.您的域名.com` 就能访问官网了
4. **强制 HTTPS**（很重要）：在 Cloudflare 左侧「**SSL/TLS**」→「**边缘证书**」里，把「**始终使用 HTTPS**」开关打开。这样别人输 http 也会自动跳到 https，更安全、对搜索更友好

---

## 一个现实提醒（重要）

您域名虽已备案，但 Cloudflare 在**中国大陆没有加速节点**，CNAME 指向的是 Cloudflare 境外节点。  
国内工厂老板打开您的网址，可能**偶尔偏慢**或个别地区不稳定。

如果上线后觉得慢，更稳的方案是换 **腾讯云 EdgeOne**（国内节点 + 备案接入，大陆速度更好，也是免费额度够用）。  
要切换的话跟我说，我给您出具体步骤——代码不用改，只是换托管。

---

## 做完后告诉我

加完解析、Cloudflare 显示 Active 后，把您的域名（如 `www.您的域名.com`）发我，  
我帮您线上验证：能不能打开、HTTPS 是否正常、手机/平板/电脑三端显示对不对。

