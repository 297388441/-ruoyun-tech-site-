# 修复 www.ryise.cn 绑定到 Cloudflare Pages 的纯中文步骤

## 当前问题
Cloudflare 提示：
> Hostname 'www.ryise.cn' already has externally managed DNS records (A, CNAME, etc). Delete them first or try a different hostname.

**原因**：您的域名 `ryise.cn` 的实际 DNS 服务器（NS）已经是 Cloudflare 的 `sasha.ns.cloudflare.com` / `byron.ns.cloudflare.com`，所以解析权在 Cloudflare 手里。您在阿里云解析里添加的 `www` 记录属于"外部记录"，Cloudflare 不认，因此报错。

## 解决思路
1. 在 Cloudflare 后台把 `ryise.cn` 作为站点添加进去（如果还没加）
2. 在 Cloudflare 的 DNS 面板里直接添加 `www` 的 CNAME 记录
3. 删除阿里云里那条 `www` 的 CNAME 记录（避免冲突）
4. 回 Cloudflare Pages 重新添加自定义域 `www.ryise.cn`

---

## 第一步：确认 ryise.cn 已在 Cloudflare 站点列表

1. 打开 https://dash.cloudflare.com 登录
2. 看左上角或首页的"站点"列表
3. 找有没有 `ryise.cn`
   - **如果有**：直接点进去，跳到第二步
   - **如果没有**：点"添加站点" → 输入 `ryise.cn` → 选择免费计划 → 继续
     - Cloudflare 会自动扫描现有记录，您只需要确认有 `www` 这一条（没有也没关系，第二步手动加）
     - 然后 Cloudflare 会给您两个 NS 地址，应该就是：
       - `sasha.ns.cloudflare.com`
       - `byron.ns.cloudflare.com`
     - 去域名注册商后台，把 DNS 服务器改成这两个（如果已经是这两个，就不用改）
     - 等 10-60 分钟生效

---

## 第二步：在 Cloudflare DNS 面板添加 www 记录

1. 进入 `ryise.cn` 站点的管理页
2. 左侧菜单点 **DNS** → **记录**
3. 点蓝色按钮 **添加记录**
4. 按下面填：
   - **类型**：`CNAME`
   - **名称**：`www`
   - **目标**：`ruoyun-tech-site.297388441.workers.dev`
   - **代理状态**：保持橙色云朵（已代理），不要点成灰色
   - **TTL**：自动
5. 点 **保存**

---

## 第三步：删除阿里云里的冲突记录

1. 登录阿里云控制台
2. 进入您截图的那个域名解析页面
3. 找到主机记录为 `www`、类型为 `CNAME` 的那条记录
4. 点右侧的 **删除**
5. 确认删除

> 删了不影响访问，因为解析权已经在 Cloudflare 了，阿里云这条本来就没生效。

---

## 第四步：回 Cloudflare Pages 添加自定义域

1. 打开 https://dash.cloudflare.com
2. 左侧 **Workers 和 Pages** → 点您的项目 `ruoyun-tech-site`
3. 顶部点 **自定义域**
4. 点 **设置自定义域**
5. 输入：`www.ryise.cn`
6. 点 **添加域名**
7. 这次应该不再报红字错误，显示"正在验证"或"Active"
8. 等 1-5 分钟，状态变成 **Active**，SSL 证书显示 **有效**

---

## 第五步：验证是否绑定成功

1. 浏览器打开：https://www.ryise.cn
2. 应该能看到和 https://ruoyun-tech-site.297388441.workers.dev 一样的网站
3. 地址栏显示一把小锁（HTTPS 正常）

---

## 如果还是报错，备选方案

如果 Cloudflare Pages 一直提示外部记录冲突，说明 `ryise.cn` 这个根域名的 NS 还没完全切到 Cloudflare。此时：

1. 去您的**域名注册商后台**（不是阿里云解析，是买域名的那个地方）
2. 找到"DNS 服务器"或"域名服务器"设置
3. 改成：
   - `sasha.ns.cloudflare.com`
   - `byron.ns.cloudflare.com`
4. 保存后等 30-60 分钟再重复第二步到第四步

---

## 完成后告诉我

1. 您在 Cloudflare 添加 `www.ryise.cn` 后，状态是不是变成 Active？
2. 浏览器打开 https://www.ryise.cn 能不能看到网站？
3. 如果还有红字报错，截图发我，我接着帮您看
